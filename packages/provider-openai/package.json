{
  "name": "@stubshot/provider-openai",
  "version": "0.1.2",
  "description": "BYOK OpenAI Stubshot provider (image generation)",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "prebuild": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "node --test ./test/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@stubshot/core": "workspace:*",
    "typescript": "^5.5.4"
  }
}
